export default function Loading() {
  return (
    <span
      className="inline-block h-3 w-3 animate-spin rounded-full border-2 border-gray-300 border-t-black"
      aria-label="Loading"
    />
  );
}
